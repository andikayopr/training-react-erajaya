import React from 'react'

function Button2(isClicked, handleClick) {
    return (
        <div>
            {/* <h1>{isClicked ? "Pressed" : "Released"}</h1>
            <button onClick={handleClick}>Press Me</button> */}
        </div>
    )
}

export default Button2